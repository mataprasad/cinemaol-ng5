import { AppDateCustomPipe } from './app-date-custom.pipe';

describe('AppDateCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new AppDateCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
